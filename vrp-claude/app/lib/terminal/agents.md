# Terminal command implementation

Before changing function codes, aliases, navigation or page adapters, read
[the portable terminal AI guide](../../../public/terminal/agents.md).
It defines the registry, parser, ticker/provider contracts, browser history and verification.
The registry lives in `commands.ts`. Keep new functions backed by actual rendered pages.
